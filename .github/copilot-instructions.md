# Project Guidelines

## Validation

Before reporting a task as complete, always verify JavaScript and HTML syntax by running:

```
npx @biomejs/biome check --diagnostic-level=error .
```

If this command outputs any errors, fix them before reporting success.
